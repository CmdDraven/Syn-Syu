#!/usr/bin/env bash
# Minimal wrapper: delegate to synsyu_core for all CLI handling.
set -euo pipefail

TARGET="${SYN_CORE_BIN:-$(command -v synsyu_core 2>/dev/null || true)}"
if [ -z "$TARGET" ]; then
  TARGET="/usr/bin/synsyu_core"
fi

exec "$TARGET" "$@"
